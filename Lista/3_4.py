## Dado que  estejam dipostos x,y respectivamente em uma Lista Simplesmente Encadeada
# Percorrer a lista até encontrar o elemento cujo 'next' seja x
# Salvar o elemento x em uma variável (x = elemento.next)
# Salvar o elemento y em uma variável (y = elemento.next.next)
# Definir que o next do elemento anterior a x seja seu next do next (elemento.next = elemento.next.next)
# O elemento referente ao next.next irá receber x (elemento.next.next = x) 
# Define o next do atual x (elemento.next.next.next = y.next)

## Dado que estejam dispostos x,y respectivamente em uma Lista Duplamente Encadeada
# 